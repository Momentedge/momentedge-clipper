/target
.omc/

# cargo-llvm-cov output (README "Tests and coverage")
/lcov.info

# Continuous rosbag2 recordings (5 s splits for edgestream-rec, one growing
# file for edgestream-rec-cont) and the clips the triggered recorders cut out
# of them — all local runtime data.
/record/
/triggered/
/record-cont/
/triggered-cont/

# Python launch-file bytecode (sim/launch)
__pycache__/
*.pyc

# bag output when `sim/cam_sim.sh record` is run from inside sim/
/sim/record/

# colcon overlay built on the target by scripts/build-on-target.sh
# (edgestream_msgs typesupport) — generated, not source.
/build/
/install/
/log/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# nix build output symlink
/result
