/target
.omc/

# Continuous rosbag2 recording (5 s circular splits) and the clips the
# triggered recorder cuts out of it — both are local runtime data.
/record/
/triggered/

# colcon overlay built on the target by scripts/build-on-target.sh
# (edgestream_msgs typesupport) — generated, not source.
/build/
/install/
/log/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/
