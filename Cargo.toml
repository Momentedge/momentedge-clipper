[package]
name = "ros2_subscribe"
version = "0.0.1"
edition = "2024"

[dependencies]
# r2r: ROS2 client bindings. `subscribe_untyped` lets us attach to a topic
# given only its type name at runtime, decoding messages to serde_json::Value.
r2r = "0.9.5"
futures = "0.3"
serde_json = "1"
